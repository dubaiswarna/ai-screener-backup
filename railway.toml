[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run enhanced_screener.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

